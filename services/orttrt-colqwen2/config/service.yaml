host: 0.0.0.0
port: 8010

model_dir: /models/colqwen2-1.0
# onnx_path: /models/colqwen2-1.0/model.onnx

provider: tensorrt   # tensorrt|cuda|cpu

# TensorRT EP options
trt_fp16: 1
trt_engine_cache: /cache/trt_engines
trt_timing_cache: /cache/trt_timing.cache

# Output behavior
pooling: none   # none|cls|mean
norm: 1